import { IOColors, IOFontSize, IOFontWeight } from "@io-app/design-system";

/**
 * Typographic defaults shared by the platform implementations, so the animated
 * iOS text and the static baseline can never drift apart.
 */
export const DEFAULT_FONT_SIZE: IOFontSize = 28;
export const DEFAULT_FONT_WEIGHT: IOFontWeight = "Semibold";
export const DEFAULT_COLOR: IOColors = "black";
