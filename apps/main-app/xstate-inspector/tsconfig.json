{
  "extends": ["../../../tsconfig.base.json", "@react-native/typescript-config"],
  "compilerOptions": {
    "checkJs": true,
    // The app pins @types/node 10 for the bundler it ships; this dev-server code
    // needs the current one, so it reads the root copy instead.
    "typeRoots": ["../../../node_modules/@types"],
    "types": ["node"]
  },
  "include": ["./metro.js", "./middleware.js"]
}
