diff --git a/dist/index.d.mts b/dist/index.d.mts
index 492fd06a36a0a80cd0cdb122d13df4fd07937a7f..8f7031594cdbf8e2f93ae1220b03db9e09d03351 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -9892,6 +9892,12 @@ interface FetchMetadataOptions {
      * The well-known paths are appended automatically.
      */
     credentialIssuerUrl: string;
+    /**
+     * Optional Authorization Server URL selected from a credential offer.
+     * When provided, it must be a valid HTTPS URL and exactly match one of
+     * the Credential Issuer metadata authorization_servers.
+     */
+    authorizationServer?: string;
 }
 /**
  * Performs the OID4VCI discovery flow for a Credential Issuer, routing discovery
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 492fd06a36a0a80cd0cdb122d13df4fd07937a7f..8f7031594cdbf8e2f93ae1220b03db9e09d03351 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -9892,6 +9892,12 @@ interface FetchMetadataOptions {
      * The well-known paths are appended automatically.
      */
     credentialIssuerUrl: string;
+    /**
+     * Optional Authorization Server URL selected from a credential offer.
+     * When provided, it must be a valid HTTPS URL and exactly match one of
+     * the Credential Issuer metadata authorization_servers.
+     */
+    authorizationServer?: string;
 }
 /**
  * Performs the OID4VCI discovery flow for a Credential Issuer, routing discovery
diff --git a/dist/index.js b/dist/index.js
index a1cdfb4b4a158c78d7da02fba713eca623a235ef..31b82ee32fcd8c7ffe54ff1eb2f7be6ce7cb2a56 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1403,7 +1403,56 @@ async function tryFederationDiscovery(fetch, baseUrl, verifyJwt4) {
     return void 0;
   }
 }
-async function fallbackDiscovery(fetch, baseUrl) {
+function selectAuthorizationServer(authorizationServers, authorizationServer) {
+  const selectedAuthorizationServer = authorizationServer ?? authorizationServers[0];
+  const parsedUrl = import_zod13.default.url().safeParse(selectedAuthorizationServer);
+  if (!parsedUrl.success || !parsedUrl.data.startsWith("https://")) {
+    throw new import_io_wallet_utils13.ValidationError(
+      "authorizationServer is not a valid HTTPS URL"
+    );
+  }
+  if (authorizationServer && !authorizationServers.includes(authorizationServer)) {
+    throw new import_io_wallet_utils13.ValidationError(
+      "authorizationServer must match one of the Credential Issuer metadata authorization_servers"
+    );
+  }
+  return parsedUrl.data;
+}
+async function fetchAuthorizationServerMetadata(fetch, authorizationServer) {
+  const authServerUrl = new URL(
+    ".well-known/oauth-authorization-server",
+    ensureTrailingSlash(authorizationServer)
+  ).toString();
+  const authServerResponse = await fetch(authServerUrl);
+  await (0, import_io_wallet_utils13.hasStatusOrThrow)(200, import_io_wallet_utils13.UnexpectedStatusCodeError)(authServerResponse);
+  return await authServerResponse.json();
+}
+async function applyAuthorizationServerSelection(fetch, raw, authorizationServer) {
+  const authorizationServers = raw.metadata.openid_credential_issuer?.authorization_servers;
+  if (authorizationServers && authorizationServers.length > 0) {
+    const selectedAuthorizationServer = selectAuthorizationServer(
+      authorizationServers,
+      authorizationServer
+    );
+    return {
+      ...raw,
+      metadata: {
+        ...raw.metadata,
+        oauth_authorization_server: await fetchAuthorizationServerMetadata(
+          fetch,
+          selectedAuthorizationServer
+        )
+      }
+    };
+  }
+  if (authorizationServer) {
+    throw new import_io_wallet_utils13.ValidationError(
+      "authorizationServer must match one of the Credential Issuer metadata authorization_servers"
+    );
+  }
+  return raw;
+}
+async function fallbackDiscovery(fetch, baseUrl, authorizationServer) {
   const issuerUrl = new URL(
     ".well-known/openid-credential-issuer",
     ensureTrailingSlash(baseUrl)
@@ -1415,32 +1464,13 @@ async function fallbackDiscovery(fetch, baseUrl) {
     await issuerResponse.json(),
     "Failed to parse credential issuer metadata"
   );
-  const authorizationServers = issuerJson.authorization_servers;
-  let oauthAuthorizationServer;
-  if (authorizationServers && authorizationServers.length > 0) {
-    const parsedUrl = import_zod13.default.url().safeParse(authorizationServers[0]);
-    if (!parsedUrl.success || !parsedUrl.data.startsWith("https://")) {
-      throw new import_io_wallet_utils13.ValidationError(
-        "authorization_servers[0] is not a valid HTTPS URL"
-      );
-    }
-    const authServerUrl = new URL(
-      ".well-known/oauth-authorization-server",
-      ensureTrailingSlash(parsedUrl.data)
-    ).toString();
-    const authServerResponse = await fetch(authServerUrl);
-    await (0, import_io_wallet_utils13.hasStatusOrThrow)(200, import_io_wallet_utils13.UnexpectedStatusCodeError)(authServerResponse);
-    oauthAuthorizationServer = await authServerResponse.json();
-  } else {
-    oauthAuthorizationServer = issuerJson;
-  }
-  return {
+  return await applyAuthorizationServerSelection(fetch, {
     discoveredVia: "oid4vci",
     metadata: {
-      oauth_authorization_server: oauthAuthorizationServer,
+      oauth_authorization_server: issuerJson,
       openid_credential_issuer: issuerJson
     }
-  };
+  }, authorizationServer);
 }
 async function fetchMetadata(options) {
   const { config } = options;
@@ -1475,7 +1505,7 @@ async function fetchMetadata(options) {
         options.credentialIssuerUrl,
         options.callbacks.verifyJwt
       );
-      const raw = federationResult ?? await fallbackDiscovery(fetch, options.credentialIssuerUrl);
+      const raw = federationResult ? await applyAuthorizationServerSelection(fetch, federationResult, options.authorizationServer) : await fallbackDiscovery(fetch, options.credentialIssuerUrl, options.authorizationServer);
       return (0, import_io_wallet_utils13.parseWithErrorHandling)(
         zMetadataResponseV1_3,
         raw,
diff --git a/dist/index.mjs b/dist/index.mjs
index 7f720cf46f4cd9d3a06d5d323ad8cc145f1909dd..a77fbe689a447ae1d82cf7707ca870c9eb7b0c6a 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1395,7 +1395,56 @@ async function tryFederationDiscovery(fetch, baseUrl, verifyJwt4) {
     return void 0;
   }
 }
-async function fallbackDiscovery(fetch, baseUrl) {
+function selectAuthorizationServer(authorizationServers, authorizationServer) {
+  const selectedAuthorizationServer = authorizationServer ?? authorizationServers[0];
+  const parsedUrl = z13.url().safeParse(selectedAuthorizationServer);
+  if (!parsedUrl.success || !parsedUrl.data.startsWith("https://")) {
+    throw new ValidationError9(
+      "authorizationServer is not a valid HTTPS URL"
+    );
+  }
+  if (authorizationServer && !authorizationServers.includes(authorizationServer)) {
+    throw new ValidationError9(
+      "authorizationServer must match one of the Credential Issuer metadata authorization_servers"
+    );
+  }
+  return parsedUrl.data;
+}
+async function fetchAuthorizationServerMetadata(fetch, authorizationServer) {
+  const authServerUrl = new URL(
+    ".well-known/oauth-authorization-server",
+    ensureTrailingSlash(authorizationServer)
+  ).toString();
+  const authServerResponse = await fetch(authServerUrl);
+  await hasStatusOrThrow4(200, UnexpectedStatusCodeError4)(authServerResponse);
+  return await authServerResponse.json();
+}
+async function applyAuthorizationServerSelection(fetch, raw, authorizationServer) {
+  const authorizationServers = raw.metadata.openid_credential_issuer?.authorization_servers;
+  if (authorizationServers && authorizationServers.length > 0) {
+    const selectedAuthorizationServer = selectAuthorizationServer(
+      authorizationServers,
+      authorizationServer
+    );
+    return {
+      ...raw,
+      metadata: {
+        ...raw.metadata,
+        oauth_authorization_server: await fetchAuthorizationServerMetadata(
+          fetch,
+          selectedAuthorizationServer
+        )
+      }
+    };
+  }
+  if (authorizationServer) {
+    throw new ValidationError9(
+      "authorizationServer must match one of the Credential Issuer metadata authorization_servers"
+    );
+  }
+  return raw;
+}
+async function fallbackDiscovery(fetch, baseUrl, authorizationServer) {
   const issuerUrl = new URL(
     ".well-known/openid-credential-issuer",
     ensureTrailingSlash(baseUrl)
@@ -1407,32 +1456,13 @@ async function fallbackDiscovery(fetch, baseUrl) {
     await issuerResponse.json(),
     "Failed to parse credential issuer metadata"
   );
-  const authorizationServers = issuerJson.authorization_servers;
-  let oauthAuthorizationServer;
-  if (authorizationServers && authorizationServers.length > 0) {
-    const parsedUrl = z13.url().safeParse(authorizationServers[0]);
-    if (!parsedUrl.success || !parsedUrl.data.startsWith("https://")) {
-      throw new ValidationError9(
-        "authorization_servers[0] is not a valid HTTPS URL"
-      );
-    }
-    const authServerUrl = new URL(
-      ".well-known/oauth-authorization-server",
-      ensureTrailingSlash(parsedUrl.data)
-    ).toString();
-    const authServerResponse = await fetch(authServerUrl);
-    await hasStatusOrThrow4(200, UnexpectedStatusCodeError4)(authServerResponse);
-    oauthAuthorizationServer = await authServerResponse.json();
-  } else {
-    oauthAuthorizationServer = issuerJson;
-  }
-  return {
+  return await applyAuthorizationServerSelection(fetch, {
     discoveredVia: "oid4vci",
     metadata: {
-      oauth_authorization_server: oauthAuthorizationServer,
+      oauth_authorization_server: issuerJson,
       openid_credential_issuer: issuerJson
     }
-  };
+  }, authorizationServer);
 }
 async function fetchMetadata(options) {
   const { config } = options;
@@ -1467,7 +1497,7 @@ async function fetchMetadata(options) {
         options.credentialIssuerUrl,
         options.callbacks.verifyJwt
       );
-      const raw = federationResult ?? await fallbackDiscovery(fetch, options.credentialIssuerUrl);
+      const raw = federationResult ? await applyAuthorizationServerSelection(fetch, federationResult, options.authorizationServer) : await fallbackDiscovery(fetch, options.credentialIssuerUrl, options.authorizationServer);
       return parseWithErrorHandling7(
         zMetadataResponseV1_3,
         raw,
