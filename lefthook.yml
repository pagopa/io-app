pre-commit:
  jobs:
    - name: lint staged files
      run: pnpm lint-staged --allow-empty

pre-push:
  parallel: true
  jobs:
    - name: validate affected projects
      run: pnpm nx affected --target=lint,tsc-noemit --parallel --nx-bail

    - name: check formatting
      run: pnpm prettier:check
