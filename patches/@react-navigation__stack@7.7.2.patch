diff --git a/lib/module/views/Stack/CardContainer.js b/lib/module/views/Stack/CardContainer.js
index dfaf18aaccb6e16cd96a69778b16e09b6f95839e..48daa1f0b5554546aa4b935cfeb66022456ab4f8 100644
--- a/lib/module/views/Stack/CardContainer.js
+++ b/lib/module/views/Stack/CardContainer.js
@@ -3,7 +3,7 @@
 import { getHeaderTitle, HeaderBackContext, HeaderHeightContext, HeaderShownContext } from '@react-navigation/elements';
 import { useLinkBuilder, useLocale, useTheme } from '@react-navigation/native';
 import * as React from 'react';
-import { StyleSheet, View } from 'react-native';
+import { StyleSheet, View, AccessibilityInfo } from 'react-native';
 import { ModalPresentationContext } from "../../utils/ModalPresentationContext.js";
 import { useKeyboardManager } from "../../utils/useKeyboardManager.js";
 import { Card } from "./Card.js";
@@ -135,6 +135,24 @@ function CardContainerInner({
   const {
     colors
   } = useTheme();
+  const [isScreenReaderEnabled, setIsScreenReaderEnabled] = React.useState(false);
+  React.useEffect(() => {
+    let isMounted = true;
+    AccessibilityInfo.isScreenReaderEnabled().then(enabled => {
+      if (isMounted) {
+        setIsScreenReaderEnabled(enabled);
+      }
+    }).catch(() => {
+      // Swallow rejection: in environments without the native module
+      // (e.g. Jest), the promise rejects with null and would otherwise
+      // surface as an unhandled rejection during test teardown.
+    });
+    const subscription = AccessibilityInfo.addEventListener('screenReaderChanged', setIsScreenReaderEnabled);
+    return () => {
+      isMounted = false;
+      subscription.remove();
+    };
+  }, []);
   React.useEffect(() => {
     const listener = scene.progress.next?.addListener?.(({
       value
@@ -161,6 +179,7 @@ function CardContainerInner({
     gestureVelocityImpact,
     headerMode,
     headerShown,
+    headerTransparent,
     transitionSpec
   } = scene.descriptor.options;
   const {
@@ -242,7 +261,10 @@ function CardContainerInner({
             getPreviousScene,
             getFocusedRoute,
             onContentHeightChange: onHeaderHeightChange,
-            style: styles.header
+            style: [styles.header, headerTransparent && isScreenReaderEnabled ? {
+              // Specify an explicit min height for Android screen readers
+              minHeight: headerHeight
+            } : null]
           }) : null, /*#__PURE__*/_jsx(View, {
             style: styles.scene,
             children: /*#__PURE__*/_jsx(HeaderBackContext.Provider, {
diff --git a/src/views/Stack/CardContainer.tsx b/src/views/Stack/CardContainer.tsx
index da56613edb9c0df85338e9aa3de06e48f9fa04c2..378a79a6590445481c1a25ddbe0024b0d25bf35c 100644
--- a/src/views/Stack/CardContainer.tsx
+++ b/src/views/Stack/CardContainer.tsx
@@ -11,7 +11,7 @@ import {
   useTheme,
 } from '@react-navigation/native';
 import * as React from 'react';
-import { Animated, StyleSheet, View } from 'react-native';
+import { AccessibilityInfo, Animated, StyleSheet, View } from 'react-native';
 
 import type { Layout, Scene } from '../../types';
 import { ModalPresentationContext } from '../../utils/ModalPresentationContext';
@@ -176,6 +176,34 @@ function CardContainerInner({
   };
 
   const { colors } = useTheme();
+  const [isScreenReaderEnabled, setIsScreenReaderEnabled] =
+    React.useState(false);
+
+  React.useEffect(() => {
+    let isMounted = true;
+
+    AccessibilityInfo.isScreenReaderEnabled()
+      .then((enabled) => {
+        if (isMounted) {
+          setIsScreenReaderEnabled(enabled);
+        }
+      })
+      .catch(() => {
+        // Swallow rejection: in environments without the native module
+        // (e.g. Jest), the promise rejects with null and would otherwise
+        // surface as an unhandled rejection during test teardown.
+      });
+
+    const subscription = AccessibilityInfo.addEventListener(
+      'screenReaderChanged',
+      setIsScreenReaderEnabled
+    );
+
+    return () => {
+      isMounted = false;
+      subscription.remove();
+    };
+  }, []);
 
   React.useEffect(() => {
     const listener = scene.progress.next?.addListener?.(
@@ -205,6 +233,7 @@ function CardContainerInner({
     gestureVelocityImpact,
     headerMode,
     headerShown,
+    headerTransparent,
     transitionSpec,
   } = scene.descriptor.options;
 
@@ -297,7 +326,15 @@ function CardContainerInner({
                   getPreviousScene,
                   getFocusedRoute,
                   onContentHeightChange: onHeaderHeightChange,
-                  style: styles.header,
+                  style: [
+                    styles.header,
+                    headerTransparent && isScreenReaderEnabled
+                      ? {
+                          // Specify an explicit min height for Android screen readers
+                          minHeight: headerHeight,
+                        }
+                      : null,
+                  ],
                 })
               : null}
             <View style={styles.scene}>
