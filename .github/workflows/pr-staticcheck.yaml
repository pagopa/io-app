name: pr-staticcheck
on:
  pull_request
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: pagopa/io-app/.github/workflows/setup.yml@setup
      - id: compile-checks
        run: yarn tsc:noemit
  prettier-check:
    runs-on: ubuntu-latest
    steps:
      - uses: pagopa/io-app/.github/workflows/setup.yml@setup
      - id: prettier-checks
        run: yarn prettier:check
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: pagopa/io-app/.github/workflows/setup.yml@setup
      - id: lint
        run: yarn lint
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: pagopa/io-app/.github/workflows/setup.yml@setup
      - id: jest-runner
        run: yarn test:ci