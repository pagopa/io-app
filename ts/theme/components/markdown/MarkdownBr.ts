import { Theme } from "../../types";

export default (): Theme => {
  return {
    "NativeBase.Text": {
      flexBasis: "100%",
      height: 0,
      overflow: "hidden",
      width: 0
    }
  };
};
