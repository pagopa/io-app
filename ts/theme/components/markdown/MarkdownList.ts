import { Theme } from "../../types";

export default (): Theme => {
  return {
    "NativeBase.ViewNB": {
      // List item
      "NativeBase.ViewNB": {
        alignItems: "center",
        flexDirection: "row",
        paddingLeft: 10
      }
    }
  };
};
