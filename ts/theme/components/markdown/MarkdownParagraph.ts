import { Theme } from "../../types";

export default (): Theme => {
  return {
    "NativeBase.ViewNB": {
      alignItems: "center",
      flexDirection: "row",
      flexWrap: "wrap",
      justifyContent: "flex-start",
      paddingBottom: 20
    }
  };
};
