/**
 * This is just a temporary file to define utility types to develop Bonus section
 */
export type Bonus = {
  type: string;
  code: string;
  max_amount: number;
  tax_benefit: number;
  activated_at: Date;
};
