import * as React from "react";
import { View } from "react-native";

/**
 * This screen allows the user to declare the required conditions
 */
export const DeclarationScreen: React.FunctionComponent = () => <View />;
