import * as React from "react";
import { View } from "react-native";

/**
 * This view is used during the loading of the eligibility of the user
 */
export const LoadEligibilityScreen: React.FunctionComponent = () => <View />;
