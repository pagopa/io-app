import * as React from "react";
import { View } from "react-native";

/**
 * This screen is displayed during the activation of the bpd
 */
export const LoadActivateBpdScreen: React.FunctionComponent = () => <View />;
