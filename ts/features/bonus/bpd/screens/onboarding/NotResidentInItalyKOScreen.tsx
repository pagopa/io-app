import * as React from "react";
import { View } from "react-native";

/**
 * This screen warns the user that he cannot participate in the program if he is not resident in Italy.
 */
export const NotResidentInItalyKoScreen: React.FunctionComponent = () => (
  <View />
);
