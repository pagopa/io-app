import * as React from "react";
import { View } from "react-native";

/**
 * This screen shows the KO page with description when the user is underage.
 */
export const EligibilityKoUnderageScreen: React.FunctionComponent = () => (
  <View />
);
