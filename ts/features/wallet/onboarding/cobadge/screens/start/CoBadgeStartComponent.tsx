import { View } from "native-base";
import * as React from "react";

type Props = unknown;

/**
 * Common component to use in different start screens
 * @param _
 * @constructor
 */
export const CoBadgeStartComponent = (_: Props): React.ReactElement => <View />;
