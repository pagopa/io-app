import { delay } from "redux-saga/effects";

/**
 * Load all the user BancomatPay accounts
 */
export function* loadBPay() {
  yield delay(0);
}

/**
 * Add bpay account to the wallet
 */
export function* addBPayToWallet() {
  yield delay(0);
}
