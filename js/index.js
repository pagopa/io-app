/**
 * Main app entrypoint
 *
 * @flow
 */

'use strict'

import { AppRegistry } from 'react-native'
import App from './App'

AppRegistry.registerComponent('ItaliaApp', (): React$ComponentType<*> => App)
